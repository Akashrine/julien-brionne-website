---
import ScrollArrow from './ScrollArrow.astro';
import Button from './Button.astro';
---

<section class="min-h-[95vh] flex flex-col items-center justify-center pt-32 pb-20 relative bg-ivory dark:bg-graphite overflow-hidden" aria-label="Section principale - Présentation">
	<div class="absolute inset-0 bg-split-overlay pointer-events-none"></div>
	
	<!-- SVG d'arrière-plan : Courbes et Cercle sur toute la largeur -->
	<div class="absolute inset-0 z-0 pointer-events-none flex items-center justify-center overflow-hidden">
		<svg width="100%" height="100%" viewBox="0 0 1440 800" preserveAspectRatio="xMidYMid slice" fill="none" xmlns="http://www.w3.org/2000/svg" class="opacity-60 dark:opacity-30">
			<!-- Courbes Fluides (traversant de gauche à droite) -->
			<path d="M-100 600 C 200 600, 400 300, 800 400 C 1200 500, 1400 200, 1600 200" stroke="#C6A676" stroke-width="1.5" fill="none" />
			<path d="M-100 620 C 200 620, 400 320, 800 420 C 1200 520, 1400 220, 1600 220" stroke="#C6A676" stroke-width="1" fill="none" opacity="0.7" />
			<path d="M-100 640 C 200 640, 400 340, 800 440 C 1200 540, 1400 240, 1600 240" stroke="#C6A676" stroke-width="0.5" fill="none" opacity="0.4" />
			
			<!-- Cercle Doré (Point de focus) -->
			<circle cx="1100" cy="300" r="15" fill="#C6A676" />
		</svg>
	</div>
	
	<div class="max-w-[1400px] mx-auto px-6 md:px-12 w-full grid grid-cols-1 lg:grid-cols-12 gap-12 items-center h-full relative z-10 flex-1">
		<div class="lg:col-span-7 space-y-8 fade-in-up text-left pr-0 lg:pr-12 delay-100">
			<h1 class="font-serif text-5xl md:text-6xl lg:text-7xl leading-[1.1] text-balance font-medium text-graphite dark:text-ivory">
				Le vrai risque, c'est la mauvaise direction.
			</h1>
			<p class="font-sans text-softgray dark:text-warmgray-400 text-lg md:text-xl max-w-xl leading-relaxed pt-2">
				Vous êtes CEO d'une SaaS B2B post-Seed ou Série A.<br>
				La roadmap avance, mais les décisions deviennent coûteuses.<br>
				Mon rôle est d'identifier le mauvais problème et de forcer une décision claire, en 4 semaines.
			</p>
			<div class="pt-8 flex flex-col sm:flex-row gap-4">
				<Button 
					href="https://app.lemcal.com/@productcopilot/decouverte"
					text="Vérifier si vous résolvez le bon problème"
					variant="primary"
					size="sm"
					lemcal={true}
					center={true}
					ariaLabel="Vérifier si vous résolvez le bon problème"
				/>
				<Button 
					href="/accompagnement/impact-sprint"
					text="Découvrir l'Impact Sprint"
					variant="outline"
					size="sm"
					center={true}
					ariaLabel="Découvrir l'Impact Sprint"
				/>
			</div>
		</div>
	</div>
	
	<ScrollArrow targetId="situation-section" />
</section>

