---
import Button from './Button.astro';
import BackgroundOverlay from './BackgroundOverlay.astro';
---

<section id="contact" class="py-32 px-6 md:px-12 flex flex-col justify-center items-center text-center relative z-0 bg-ivory dark:bg-graphite-light fade-in-up" aria-labelledby="contact-heading">
	<BackgroundOverlay variant="sand" />
			<div class="max-w-3xl space-y-6 relative z-10">
				<h2 id="contact-heading" class="font-serif text-3xl md:text-4xl leading-tight text-graphite dark:text-ivory mb-4">
					Votre produit commence à dériver.
				</h2>
				<p class="text-base md:text-lg text-graphite dark:text-warmgray-300 mb-6">
					Vérifiez si vous êtes en train de résoudre le bon problème avant que les décisions ne coûtent trop cher.
				</p>
				<div class="flex flex-col items-center gap-6 pt-4">
					<Button 
						href="https://app.lemcal.com/@productcopilot/decouverte"
						text="Programmer un diagnostic produit →"
						variant="primary"
						size="md"
						lemcal={true}
						ariaLabel="Programmer un diagnostic produit"
					/>
					<div class="w-16 h-0.5 bg-sand mx-auto opacity-40"></div>
				</div>
			</div>
</section>

